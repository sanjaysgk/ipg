process {
    withName: 'ENSEMBLVEP_VEP' {
        ext.prefix = { "${meta.id}.vep" }
        ext.args   = '--everything'
    }
    withName: 'SNPEFF_SNPEFF' {
        ext.prefix = { "${meta.id}.snpeff" }
    }
}
