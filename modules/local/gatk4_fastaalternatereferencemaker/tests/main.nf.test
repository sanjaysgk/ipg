nextflow_process {

    name "Test Process GATK4_FASTAALTERNATEREFERENCEMAKER"
    script "../main.nf"
    process "GATK4_FASTAALTERNATEREFERENCEMAKER"

    tag "modules"
    tag "modules_local"
    tag "gatk4_fastaalternatereferencemaker"

    test("gatk4_fastaalternatereferencemaker - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [ [ id:'test' ], file('test.vcf.gz'), file('test.vcf.gz.tbi') ]
                input[1] = [ [ id:'reference' ], file('reference.fasta') ]
                input[2] = [ [ id:'reference' ], file('reference.fasta.fai') ]
                input[3] = [ [ id:'reference' ], file('reference.dict') ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
